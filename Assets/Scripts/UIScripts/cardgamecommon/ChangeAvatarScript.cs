using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;


public class ChangeAvatarScript 
{

    /*
    public void onSelected() {


        //Get all other game object and deselect them.
        GameObject parent = GameObject.Find("avatarparent");

        foreach (Transform child in parent.transform)
        {
            

        //child is your child transform
        }


        //Get the selected playerObject.
        
        List<GameObject> currentAvatars = GameObjectFinder.playerAvatar();

        

        foreach(GameObject obj in currentAvatars) {
            if(obj.GetComponent<ChangeAvatarPlayerScript>().selected) {
                obj.GetComponent<Image>().sprite = gameObject.GetComponent<Image>().sprite;
            }
        }


    }


    public void OnPointerClick(PointerEventData eventData)
    {
        onSelected();
        // OnClick code goes here ...
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
*/

}
